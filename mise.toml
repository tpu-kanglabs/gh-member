[tools]
go = "1.25"
golangci-lint = "2.13.2"

[tasks.build]
run = "go build ."
[tasks.install]
run = "gh extension install ."
[tasks.remove]
run = "gh extension remove member"
[tasks.format]
run = "golangci-lint fmt"
[tasks.lint]
run = "golangci-lint run"
[tasks.test]
run = "go test -v ./..."
